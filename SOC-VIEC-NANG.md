# Sóc — sổ việc nặng ĐÃ DỜI, bản này không còn dùng

⛔ **Sổ đang dùng nằm ở `/Users/Huy/Claude/App/Soc/SOC-VIEC-NANG.md`** (dời chiều 09/08/2026,
cùng lúc app Sóc tách hẳn sang repo `App/Soc` và lên `https://soc-eiv.pages.dev`).
Routine `soc-nang` đọc bản kia.

Thư mục `soc/` trong repo này **đã gỡ hẳn tối 09/08/2026** sau khi 12 mảng nó có mà bản mới
chưa có đều đã bù xong. Cần đọc lại mã cũ thì lấy từ lịch sử git (`git show 7bac245:soc/index.html`)
hoặc bản sao trong `~/.Trash/soc-ban-cu-trong-JustUs-2026-08-09/`.

Phần mô tả cấu trúc bản Sóc cũ đã cắt bỏ khỏi file này — giữ lại chỉ tổ dẫn người đọc đi sửa
một app không còn tồn tại.
